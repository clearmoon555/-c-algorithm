#include<cstdio>
/*
const int maxn = 210;
int a[maxn] = {};
int main() {
	int n,x;
	while (scanf_s("%d",&n) != EOF)
	{
		for (int i = 0; i < n; i++) {
			scanf_s("%d", &a[i]);

		}
		scanf_s(" %d", &x);
		for (int k = 0; k <= n; k++) {
			if (k == n) {
				printf("-1\n");
				break;
			}
			if (a[k] == x){
				printf("%d\n", k);
				break;
			}
		}
	}
	return 0;
}
*/